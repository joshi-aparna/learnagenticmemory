Notes:
### the basic memory search was not correct. It needed exact word matching. 

Trying embeddings to see if it gets better.

Step 1:
ollama pull embeddinggemma

### added support for core memory and archival memory
no support for compressing memory - it only grows